# master-thesis-code
The codebase I created for my master thesis work. Contains software for ESP32 data collection and Python data processing.

The Jupyter folder contains two Jupyter notebooks used to train a YOLO model and apply said model to a set of images created by sampling the video stream collected. They were originally run on the Jupyter server, hence them taking on this form.
